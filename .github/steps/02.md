# Lab 07 · Step 2 — Observe a real instructor-enabled dev plan

**Goal:** Observe the first real plan/apply from your merged PR.

> **STOP — owner prerequisites:** keep `WORKSHOP_AZURE_ENABLED=false` until verified for private non-template **alvine-aurelio-org/ws2-sim-20260921-azure-delivery-laboratory-07**, ID **1379149907**: current protected main with strict required PR checks; approved subscription/RG/region/CIDRs, budget/currency, lifetime and explicit bootstrap authorization; separate scoped plan/apply OIDC identities; reachable private backend with container lease rights, separate state/single writer; restricted `ws2-trusted` runner, encryption keys and module App ready; main-only `dev-plan`/`dev-apply`, no Required reviewers and no admin bypass. [Owner configuration](../../docs/delivery-configuration.md) / [preflight](../../docs/instructor-preflight.md). Public templates/unapproved copies stay offline; never repin the allowlist.

### 1. Merge the actual authoring PR

1. Missing main? **STOP**: only the owner establishes protected main **while disabled, after readiness review**. Source maintenance stays on `dev`, not live `main`.
2. Only the owner may enable the approved live window. GitHub: **Pull requests → New pull request → base: main → compare: lab/workflow-authoring**. Inspect **Files changed**, wait for current checks, resolve conversations, then **Merge pull request → Confirm merge**. The author may merge their own passing PR; no self-approval or trigger-only commit.
3. **Actions → Trusted dev delivery (instructor enablement required)** → new **push / main** run: match **Code → main**'s current main SHA, fresh **attempt 1**. No manual deploy button, reviewer wait, second dispatch or **Re-run jobs**.

![GitHub reference: select the workflow](../../docs/images/github-workflow-sidebar.webp)

*REFERENCE — GitHub, CC BY 4.0; example labels, not execution evidence. [Attribution](../../docs/images/NOTICE.md).*

### 2. Observe the exact saved plan

Require **Verify scoped dev deployment policy → Validate reviewed delivery revision → Trusted dev plan → Apply exact dev saved plan**. Hosted validation uses this SHA without cloud credentials; automatic apply consumes the **same run's encrypted saved plan**, never a replacement.

In **Summary**, compare source/module/state/run bindings, saved-plan/manifest digests and changes. Ordinary pushes reject deletion/replacement. Plans expire after **2 hours**; **1 day** encrypted-artifact retention does not extend validity. No plaintext plans/state, keys/tokens or local apply.

### 3. Wait for the recorded checkpoint

**Expected:** Refresh Exercise after the **whole run has completed**: actual current-SHA main, attempt 1, after course start/preceding checkpoint, with successful **Trusted dev plan**, not mock/skipped/unfinished jobs. AgentAlvine observes metadata, not Azure authorization/configuration.

After Step 2 is recorded, [Step 3](03.md)'s real note PR produces a **later fresh plan/apply run**; this first run cannot count again.

**Recovery:** Give the owner the sanitized blocker; preserve controls, obtain a new authorized run, never a credentialled rerun.
